import styled from "styled-components";

export const Wrapper = styled.div`
  label {
    display: flex;
    justify-content: flex-end;
    width: 15rem;
  }

  .form-text {
    display: flex;
    justify-content: flex-end;
    width: 15rem;
  }

  .padded {
    padding-top: 2.06rem;
  }
`;
